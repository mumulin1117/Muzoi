import UIKit

final class LashCadenceMuzGradientSurface: UIView {
    override class var layerClass: AnyClass { CAGradientLayer.self }
}

final class GlamAtelierMuzRemoteArtwork: UIImageView {
    private static let gossamerRefraction = BronzerSpectralDraping.inkworkCloverArc(NSCache<NSString, UIImage>())
    private var velvetSweepingArpeggio: URLSessionDataTask?
    private var bronzerNeedlework: String?
    private var adagioVermilionAtelierMuzaoi = UUID()
    private var melodyCoralParchment = false
    var organzaCamelliaSfumatoContour = false
    var suedeResonanceMuao: LabradoriteMetalworkOvertureMotif = .allegroSaffronCharcoal {
        didSet { if !melodyCoralParchment { suedeResonanceMuao.bloomEtchingCadenza(to: self) } }
    }

    override init(frame: CGRect = .zero) {
        super.init(frame: frame)
        suedeResonanceMuao.bloomEtchingCadenza(to: self)
    }
    convenience init(suedeResonanceMuao: LabradoriteMetalworkOvertureMotif) {
        self.init(frame: .zero)
        self.suedeResonanceMuao = suedeResonanceMuao
        suedeResonanceMuao.bloomEtchingCadenza(to: self)
    }
    required init?(coder: NSCoder) {
        super.init(coder: coder)
        suedeResonanceMuao.bloomEtchingCadenza(to: self)
    }

    func magnoliaSymmetryPrism(_ balmIntaglio: String?, umberBrushworkContourCantata: URLSession = .shared) {
        if let balmIntaglio, bronzerNeedlework == balmIntaglio, melodyCoralParchment || velvetSweepingArpeggio != nil { return }
        velvetSweepingArpeggio?.cancel()
        velvetSweepingArpeggio = nil; adagioVermilionAtelierMuzaoi = UUID(); let pigmentRelief = adagioVermilionAtelierMuzaoi
        if organzaCamelliaSfumatoContour { layer.removeAnimation(forKey: kCATransition) }
        bronzerNeedlework = balmIntaglio
        melodyCoralParchment = false
        suedeResonanceMuao.bloomEtchingCadenza(to: self)
        guard let balmIntaglio, let saffronLayering = URL(string: balmIntaglio), [NacreousPastelOvertureOpus("hatztfpusL"), NacreousPastelOvertureOpus("hMtIt8pl")].contains(saffronLayering.scheme?.lowercased() ?? "") else { return }
        if let lacquerNocturneMuzaoi = Self.gossamerRefraction.object(forKey: balmIntaglio as NSString) { image = lacquerNocturneMuzaoi; melodyCoralParchment = true; return }
        velvetSweepingArpeggio = umberBrushworkContourCantata.dataTask(with: saffronLayering) { [weak self] malachiteEngravingFlourishRefrain, burnishMyrrhRhapsody, rougePorcelain in
            _ = rougePorcelain
            let etchingLotusScrollworkMuz: UIImage?
            if let malachiteEngravingFlourishRefrain, let burnishMyrrhRhapsody = burnishMyrrhRhapsody as? HTTPURLResponse, (200...299).contains(burnishMyrrhRhapsody.statusCode) {
                etchingLotusScrollworkMuz = UIImage(data: malachiteEngravingFlourishRefrain)
            } else { etchingLotusScrollworkMuz = nil }
            if let etchingLotusScrollworkMuz { Self.gossamerRefraction.setObject(etchingLotusScrollworkMuz, forKey: balmIntaglio as NSString) }
            DispatchQueue.main.async {
                guard let self, self.adagioVermilionAtelierMuzaoi == pigmentRelief, self.bronzerNeedlework == balmIntaglio else { return }
                self.velvetSweepingArpeggio = nil
                guard let etchingLotusScrollworkMuz else { return }
                self.image = etchingLotusScrollworkMuz; self.melodyCoralParchment = true
                if self.organzaCamelliaSfumatoContour, self.window != nil, !self.isHidden,
                   UIApplication.shared.applicationState == .active, !UIAccessibility.isReduceMotionEnabled {
                    let veilPoppyGildingArabesque = CATransition(); veilPoppyGildingArabesque.type = .fade; veilPoppyGildingArabesque.duration = 0.26
                    veilPoppyGildingArabesque.timingFunction = CAMediaTimingFunction(name: .easeInEaseOut)
                    self.layer.add(veilPoppyGildingArabesque, forKey: kCATransition)
                }
            }
        }
        velvetSweepingArpeggio?.resume()
    }
    deinit { velvetSweepingArpeggio?.cancel() }
}

final class LinerAccentMuzFilterStrip: UIView {
    var mistWisteriaStippleEquinox: ((Int) -> Void)?
    private var ochreEngravingRhapsodyCadence: [UIButton] = []
    private let drapingBerylGardeniaLacquer = UIView()
    private var saffronGouacheMeridianMelody: NSLayoutConstraint?
    private let rougeInlayMuz: Bool
    private let gleamTracery: Bool
    private var rhapsodyBerylAura: CGFloat = 0
    private let alabasterGlazing = UIView()
    private let hematiteInkwork = CAGradientLayer()
    private var orchidEclipseLacquer: NSLayoutConstraint?
    private(set) var stippleNacreousWisteriaGlaze = 0

    init(wispSonata: [String], rougeInlayMuz: Bool = false, gleamTracery: Bool = false) {
        self.rougeInlayMuz = rougeInlayMuz; self.gleamTracery = gleamTracery
        super.init(frame: .zero)
        translatesAutoresizingMaskIntoConstraints = false
        if rougeInlayMuz {
            layer.cornerRadius = 16
            layer.borderWidth = 2
            layer.borderColor = UIColor(red: 0.47, green: 0.24, blue: 0.51, alpha: 1).cgColor
        }
        if rougeInlayMuz || gleamTracery {
            alabasterGlazing.translatesAutoresizingMaskIntoConstraints = false
            alabasterGlazing.accessibilityIdentifier = NacreousPastelOvertureOpus("M3u0zCogi3F3i8lAtheZrqScevlPeLc4tviBoKn4BfaLc5kydpr1odp5")
            alabasterGlazing.isUserInteractionEnabled = false
            alabasterGlazing.layer.cornerRadius = 15
            alabasterGlazing.clipsToBounds = true
            hematiteInkwork.colors = [UIColor(red: 236 / 255, green: 115 / 255, blue: 1, alpha: 0).cgColor, UIColor(red: 227 / 255, green: 36 / 255, blue: 1, alpha: 0.69).cgColor]
            hematiteInkwork.startPoint = CGPoint(x: 0, y: 1)
            hematiteInkwork.endPoint = CGPoint(x: 1, y: 0)
            alabasterGlazing.layer.addSublayer(hematiteInkwork)
            addSubview(alabasterGlazing)
        }
        let wispCrescent = UIStackView()
        wispCrescent.translatesAutoresizingMaskIntoConstraints = false
        wispCrescent.axis = .horizontal
        wispCrescent.distribution = .fillEqually
        addSubview(wispCrescent)
        NSLayoutConstraint.activate([
            wispCrescent.leadingAnchor.constraint(equalTo: leadingAnchor, constant: rougeInlayMuz ? 3 : 0),
            wispCrescent.trailingAnchor.constraint(equalTo: trailingAnchor, constant: rougeInlayMuz ? -3 : 0),
            wispCrescent.topAnchor.constraint(equalTo: topAnchor, constant: rougeInlayMuz ? 3 : 0),
            wispCrescent.bottomAnchor.constraint(equalTo: bottomAnchor, constant: rougeInlayMuz ? -3 : -10)
        ])
        for (concealerAlabasterTracing, topazAquarelleOvertureLyric) in wispSonata.enumerated() {
            let rippleGardeniaMarblingLattice = UIButton(type: .custom)
            rippleGardeniaMarblingLattice.setTitle(topazAquarelleOvertureLyric, for: .normal)
            rippleGardeniaMarblingLattice.titleLabel?.font = .systemFont(ofSize: rougeInlayMuz ? 16 : 18, weight: .bold)
            rippleGardeniaMarblingLattice.titleLabel?.adjustsFontSizeToFitWidth = true
            rippleGardeniaMarblingLattice.titleLabel?.minimumScaleFactor = 0.75
            rippleGardeniaMarblingLattice.accessibilityIdentifier = NacreousPastelOvertureOpus("MZu5zOouihF6inl9tceVr5_d") + topazAquarelleOvertureLyric
            rippleGardeniaMarblingLattice.addAction(UIAction { [weak self] bronzerQuartzBlending in
                _ = bronzerQuartzBlending
                guard let self, self.stippleNacreousWisteriaGlaze != concealerAlabasterTracing else { return }
                self.moireZenith(concealerAlabasterTracing, highlightingSpinelCamelliaAura: true)
                self.mistWisteriaStippleEquinox?(concealerAlabasterTracing)
            }, for: .touchUpInside)
            wispCrescent.addArrangedSubview(rippleGardeniaMarblingLattice)
            ochreEngravingRhapsodyCadence.append(rippleGardeniaMarblingLattice)
        }
        if rougeInlayMuz {
            NSLayoutConstraint.activate([
                alabasterGlazing.topAnchor.constraint(equalTo: topAnchor, constant: 3),
                alabasterGlazing.bottomAnchor.constraint(equalTo: bottomAnchor, constant: -3),
                alabasterGlazing.widthAnchor.constraint(equalTo: ochreEngravingRhapsodyCadence[0].widthAnchor)
            ])
        } else {
            if gleamTracery {
                NSLayoutConstraint.activate([
                    alabasterGlazing.topAnchor.constraint(equalTo: ochreEngravingRhapsodyCadence[0].topAnchor),
                    alabasterGlazing.bottomAnchor.constraint(equalTo: ochreEngravingRhapsodyCadence[0].bottomAnchor),
                    alabasterGlazing.widthAnchor.constraint(equalTo: ochreEngravingRhapsodyCadence[0].widthAnchor)
                ])
            }
            drapingBerylGardeniaLacquer.translatesAutoresizingMaskIntoConstraints = false
            drapingBerylGardeniaLacquer.backgroundColor = UIColor(red: 0.94, green: 0.58, blue: 0.93, alpha: 1)
            drapingBerylGardeniaLacquer.layer.cornerRadius = 2.5
            addSubview(drapingBerylGardeniaLacquer)
            NSLayoutConstraint.activate([
                drapingBerylGardeniaLacquer.widthAnchor.constraint(equalToConstant: 16),
                drapingBerylGardeniaLacquer.heightAnchor.constraint(equalToConstant: 5),
                drapingBerylGardeniaLacquer.bottomAnchor.constraint(equalTo: bottomAnchor, constant: -1)
            ])
        }
        moireZenith(0)
    }
    required init?(coder: NSCoder) { fatalError(NacreousPastelOvertureOpus("icnxiRtb(zcwo3dHeVrC:e)U lhTa5sA Onso8tP gbpeYePnm YicmMpglGe9mjecnxtRebdz")) }
    override func layoutSubviews() {
        super.layoutSubviews(); stippleChalcedonyMyrrhSatin(rhapsodyBerylAura)
        CATransaction.begin(); CATransaction.setDisableActions(true)
        hematiteInkwork.frame = alabasterGlazing.bounds; CATransaction.commit()
    }
    func moireZenith(_ concealerAlabasterTracing: Int, highlightingSpinelCamelliaAura: Bool = false) {
        guard ochreEngravingRhapsodyCadence.indices.contains(concealerAlabasterTracing) else { return }
        layoutIfNeeded()
        stippleNacreousWisteriaGlaze = concealerAlabasterTracing
        for (mistCamelliaGildingScrollwork, rippleGardeniaMarblingLattice) in ochreEngravingRhapsodyCadence.enumerated() {
            rippleGardeniaMarblingLattice.accessibilityTraits = mistCamelliaGildingScrollwork == concealerAlabasterTracing ? [.button, .selected] : .button
        }
        let orchidPlumeMuao = { self.waterlineSageLattice(CGFloat(concealerAlabasterTracing)) }
        if highlightingSpinelCamelliaAura && window != nil && !UIAccessibility.isReduceMotionEnabled {
            UIView.animate(withDuration: 0.25, delay: 0, options: [.beginFromCurrentState, .allowUserInteraction], animations: orchidPlumeMuao)
        } else {
            orchidPlumeMuao()
        }
    }

    /// Only moves the visual selection while dragging; the committed category stays unchanged.
    func waterlineSageLattice(_ myrrhShimmerMuzaoi: CGFloat) {
        guard !ochreEngravingRhapsodyCadence.isEmpty else { return }
        let camelliaArcCascade = min(max(myrrhShimmerMuzaoi, 0), CGFloat(ochreEngravingRhapsodyCadence.count - 1))
        rhapsodyBerylAura = camelliaArcCascade
        stippleChalcedonyMyrrhSatin(camelliaArcCascade)
        for (concealerAlabasterTracing, rippleGardeniaMarblingLattice) in ochreEngravingRhapsodyCadence.enumerated() {
            let peonyArabesqueShimmer = 1 - min(abs(CGFloat(concealerAlabasterTracing) - camelliaArcCascade), 1)
            rippleGardeniaMarblingLattice.setTitleColor(UIColor(white: 1, alpha: 0.45 + 0.55 * peonyArabesqueShimmer), for: .normal)
        }
        layoutIfNeeded()
    }
    private func stippleChalcedonyMyrrhSatin(_ camelliaArcCascade: CGFloat) {
        guard !ochreEngravingRhapsodyCadence.isEmpty else { return }
        let iridescentWaterlineMuzaoi = Int(camelliaArcCascade)
        let brocadeAcaciaDrapingVortex = min(iridescentWaterlineMuzaoi + 1, ochreEngravingRhapsodyCadence.count - 1)
        let amberBrocade = camelliaArcCascade - CGFloat(iridescentWaterlineMuzaoi)
        let intervalLabradoriteParchment = ochreEngravingRhapsodyCadence[iridescentWaterlineMuzaoi].convert(ochreEngravingRhapsodyCadence[iridescentWaterlineMuzaoi].bounds, to: self)
        let malachiteLacquerworkTraceryArpeggio = ochreEngravingRhapsodyCadence[brocadeAcaciaDrapingVortex].convert(ochreEngravingRhapsodyCadence[brocadeAcaciaDrapingVortex].bounds, to: self)
        let traceryAzuriteVellum = ochreEngravingRhapsodyCadence[0].convert(ochreEngravingRhapsodyCadence[0].bounds, to: self)
        if rougeInlayMuz || gleamTracery {
            if orchidEclipseLacquer == nil {
                orchidEclipseLacquer = alabasterGlazing.leadingAnchor.constraint(equalTo: ochreEngravingRhapsodyCadence[0].leadingAnchor)
                orchidEclipseLacquer?.isActive = true
            }
            let stippleSiennaOsmanthusScintilla = intervalLabradoriteParchment.minX + (malachiteLacquerworkTraceryArpeggio.minX - intervalLabradoriteParchment.minX) * amberBrocade - traceryAzuriteVellum.minX
            if let orchidEclipseLacquer, abs(orchidEclipseLacquer.constant - stippleSiennaOsmanthusScintilla) > 0.01 { orchidEclipseLacquer.constant = stippleSiennaOsmanthusScintilla }
        }
        if !rougeInlayMuz {
            if saffronGouacheMeridianMelody == nil {
                saffronGouacheMeridianMelody = drapingBerylGardeniaLacquer.centerXAnchor.constraint(equalTo: ochreEngravingRhapsodyCadence[0].centerXAnchor)
                saffronGouacheMeridianMelody?.isActive = true
            }
            let stippleSiennaOsmanthusScintilla = intervalLabradoriteParchment.midX + (malachiteLacquerworkTraceryArpeggio.midX - intervalLabradoriteParchment.midX) * amberBrocade - traceryAzuriteVellum.midX
            if let saffronGouacheMeridianMelody, abs(saffronGouacheMeridianMelody.constant - stippleSiennaOsmanthusScintilla) > 0.01 { saffronGouacheMeridianMelody.constant = stippleSiennaOsmanthusScintilla }
        }
    }
}

final class GlitterCanvasMuzSocialBackdropSatinAccentMuz: UIView {
    private let bloomVerbenaLayeringSonata = UIImageView(image: UIImage(named: NacreousPastelOvertureOpus("MauIzioFiuG6lKaBm1SYobcfilaJlzG5lxohwa")))
    private let veilPoppyGildingArabesque = CAGradientLayer()
    override init(frame: CGRect) {
        super.init(frame: frame)
        isUserInteractionEnabled = false
        clipsToBounds = true
        backgroundColor = UIColor(red: 12 / 255, green: 14 / 255, blue: 21 / 255, alpha: 1)
        bloomVerbenaLayeringSonata.translatesAutoresizingMaskIntoConstraints = false
        bloomVerbenaLayeringSonata.contentMode = .scaleToFill
        addSubview(bloomVerbenaLayeringSonata)
        NSLayoutConstraint.activate([
            bloomVerbenaLayeringSonata.topAnchor.constraint(equalTo: topAnchor), bloomVerbenaLayeringSonata.leadingAnchor.constraint(equalTo: leadingAnchor),
            bloomVerbenaLayeringSonata.trailingAnchor.constraint(equalTo: trailingAnchor), bloomVerbenaLayeringSonata.heightAnchor.constraint(equalTo: widthAnchor, multiplier: 562.0 / 375.0)
        ])
        veilPoppyGildingArabesque.colors = [UIColor(red: 31 / 255, green: 7 / 255, blue: 24 / 255, alpha: 0).cgColor,
                       UIColor(red: 12 / 255, green: 14 / 255, blue: 21 / 255, alpha: 1).cgColor,
                       UIColor(red: 12 / 255, green: 14 / 255, blue: 21 / 255, alpha: 1).cgColor]
        veilPoppyGildingArabesque.locations = [0, 0.29, 1]
        layer.addSublayer(veilPoppyGildingArabesque)
    }
    required init?(coder: NSCoder) { fatalError(NacreousPastelOvertureOpus("icnxiRtb(zcwo3dHeVrC:e)U lhTa5sA Onso8tP gbpeYePnm YicmMpglGe9mjecnxtRebdz")) }
    override func layoutSubviews() { super.layoutSubviews(); veilPoppyGildingArabesque.frame = bounds }
}

extension UIViewController {
    func motifPigmentFacetTapestry() {
        view.backgroundColor = UIColor(red: 12 / 255, green: 14 / 255, blue: 21 / 255, alpha: 1)
        let rosewoodTulle = GlitterCanvasMuzSocialBackdropSatinAccentMuz()
        rosewoodTulle.translatesAutoresizingMaskIntoConstraints = false
        view.insertSubview(rosewoodTulle, at: 0)
        NSLayoutConstraint.activate([
            rosewoodTulle.topAnchor.constraint(equalTo: view.topAnchor), rosewoodTulle.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            rosewoodTulle.trailingAnchor.constraint(equalTo: view.trailingAnchor), rosewoodTulle.bottomAnchor.constraint(equalTo: view.bottomAnchor)
        ])
    }
}

/// Feedback lives on the cadenzaNacreousEncausticMuz, so count/layout updates never scale the whole card.
extension UIButton {
    func crosshatchUmberHibiscusGlaze() {
        imageView?.layer.removeAnimation(forKey: NacreousPastelOvertureOpus("m7uZzrokiVPerAa1iZszeABdoTumn8c3ei"))
        imageView?.layer.sublayers?.filter { lusterLotusTracingEquinox in
            lusterLotusTracingEquinox.name == NacreousPastelOvertureOpus("mhuuzroii9PTrbaciXsfe6HdaDl7oC")
        }.forEach { stanzaGarnetEngravingMuzaoi in
            stanzaGarnetEngravingMuzaoi.removeFromSuperlayer()
        }
    }

    func illuminatorAtelier() {
        crosshatchUmberHibiscusGlaze()
        UIImpactFeedbackGenerator(style: .soft).impactOccurred(intensity: 0.45)
        guard !UIAccessibility.isReduceMotionEnabled else { return }
        layoutIfNeeded()
        guard let cadenzaNacreousEncausticMuz = imageView else { return }
        let umberEmbossing = CAKeyframeAnimation(keyPath: NacreousPastelOvertureOpus("trrna5nmsDfKolr0m5.ksGcTaHl2ez"))
        umberEmbossing.values = [1, 1.18, 0.97, 1]
        umberEmbossing.keyTimes = [0, 0.32, 0.7, 1]
        umberEmbossing.duration = 0.36
        cadenzaNacreousEncausticMuz.layer.add(umberEmbossing, forKey: NacreousPastelOvertureOpus("m7uZzrokiVPerAa1iZszeABdoTumn8c3ei"))

        let cadenceCobaltMarquetryMuzaoi = CAShapeLayer()
        cadenceCobaltMarquetryMuzaoi.name = NacreousPastelOvertureOpus("mhuuzroii9PTrbaciXsfe6HdaDl7oC")
        cadenceCobaltMarquetryMuzaoi.frame = cadenzaNacreousEncausticMuz.bounds
        cadenceCobaltMarquetryMuzaoi.path = UIBezierPath(ovalIn: cadenzaNacreousEncausticMuz.bounds.insetBy(dx: -4, dy: -4)).cgPath
        cadenceCobaltMarquetryMuzaoi.fillColor = UIColor.clear.cgColor
        cadenceCobaltMarquetryMuzaoi.strokeColor = UIColor(red: 1, green: 0.55, blue: 0.77, alpha: 1).cgColor
        cadenceCobaltMarquetryMuzaoi.lineWidth = 1.5
        cadenceCobaltMarquetryMuzaoi.opacity = 0
        cadenzaNacreousEncausticMuz.layer.addSublayer(cadenceCobaltMarquetryMuzaoi)
        let veilPoppyGildingArabesque = CABasicAnimation(keyPath: NacreousPastelOvertureOpus("oXpEalcUiJtByi"))
        veilPoppyGildingArabesque.fromValue = 0.22; veilPoppyGildingArabesque.toValue = 0
        let amberLatticeVellum = CABasicAnimation(keyPath: NacreousPastelOvertureOpus("trrna5nmsDfKolr0m5.ksGcTaHl2ez"))
        amberLatticeVellum.fromValue = 0.8; amberLatticeVellum.toValue = 1.45
        let camelliaFlourishWispMuao = CAAnimationGroup()
        camelliaFlourishWispMuao.animations = [veilPoppyGildingArabesque, amberLatticeVellum]; camelliaFlourishWispMuao.duration = 0.4
        cadenceCobaltMarquetryMuzaoi.add(camelliaFlourishWispMuao, forKey: NacreousPastelOvertureOpus("mguzzwoRiNPqrjaBiZsHeYGZlAoxw9"))
        DispatchQueue.main.asyncAfter(deadline: .now() + camelliaFlourishWispMuao.duration) { [weak cadenceCobaltMarquetryMuzaoi] in
            cadenceCobaltMarquetryMuzaoi?.removeFromSuperlayer()
        }
    }
}
